/* I am attempting to create a program that can compute
 * the square root for any given integer number.
*/
import java.util.Scanner;
public class RamisAttempt{
  public static void main (String [] args) {
    Scanner std=new Scanner(System.in);
    int x,y,z,loop;
    System.out.println("Please enter an integer value");
    x=std.nextInt();
    for(loop=0;loop>=0;loop=loop+=1){
      y=z=loop;
    if(x==y*z)
      System.out.println("the square root of "+x+" is "+y);
    }}}
    
    